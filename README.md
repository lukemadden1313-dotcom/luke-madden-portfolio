# Luke Madden — Portfolio

One-page personal portfolio.

## Run locally

Any static server works. Two easy options:

```bash
# Python
python3 -m http.server 3003

# Node
npx serve .
```

Then open http://localhost:3003

## Deploy

Static. Push to GitHub and connect to Vercel — no build step required.
